name: smart_deadline
description: SmartDeadline - an offline-first smart task and deadline manager.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.5.3

flutter:
  uses-material-design: true
dev_dependencies:
  flutter_lints: ^6.0.0
